<div class="form-blog">
<%= form_for(blog) do |f| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, style: "font-size: 15px;
    font-weight: 600;" %>
    <%= f.text_field :title, style: "font-weight: 600;" %>
  </div>

  <div class="field">
    <%= f.label :body, style: "font-size: 15px;
    font-weight: 600;" %>
    <%= f.text_area :body, style: "height: 180px; font-weight: 600;" %>
  </div>

  <div class="field">
    <%= f.label :image, style: "font-size: 15px;
    font-weight: 600;" %>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit "Create Blog", style: "font-weight: 600;" %>
  </div>
<% end %>
</div>
